interface:
  display_name: "Set Up Media Server"
  short_description: "Install and verify the Docker media stack"
  default_prompt: "Use $setup-media-server to configure and verify this media-server stack on my machine."
